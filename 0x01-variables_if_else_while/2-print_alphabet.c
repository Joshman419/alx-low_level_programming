#include <stdio.h>
/**
 * main - Entry Point
 *Return: return 0 if Success and none zero if failed
 */
int main(void)
{
	char i;
	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
