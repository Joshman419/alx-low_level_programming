#include <stdio.h>
/**
 * main- Entry Point
 *Return: 0 if successful and non success if it fails
 */
int main(void)
{
	char i;

	for (i = '0'; i <= '9'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
